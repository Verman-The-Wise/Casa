import parser
import render
import shutil
from var import O
# surprisingly just importing the base, project runs them when I run main.py. 

shutil.copy("style.css", O)   # copies the style.css to output DIR.